# claudart Audit — feature/bare-string-lint

> Working tracker for this branch, not a public document. Not the same thing
> as `README.md` (current released state) or `PLAN.md` (roadmap) — see
> CLAUDE.md's document hierarchy. This file is scoped to the branch and can
> be deleted once the audit closes out; findings that should persist belong
> in PLAN.md, docs/design.md, or docs/session_log.md instead.

## Scope

Auditing claudart's session paradigm end to end: `link` → `setup` →
`suggest`/`save`/`debug` → `teardown`, plus the surrounding lifecycle
commands (`rotate`, `kill`, `preflight`, `scan`, `report`, `map`). Looking
at: where each command collects metadata, how that metadata is passed to
templates, and how session state is tracked — then tightening whatever
doesn't match its own documented intent.

## Status legend

`done` — merged to `main` · `landed` — on this branch, in PR #37 or later ·
`open` — found, not yet fixed · `deciding` — needs a call before fixing

## Findings

| # | Area | Status | Summary |
|---|------|--------|---------|
| 1 | `link.dart` | done (`main` @ `bb18297`) | Real (non-symlinked) `.claude/` never received template writes — `<projectRoot>/.claude/commands/` silently drifted stale forever. Fixed: sync into both bare and suffixed filenames when the symlink is skipped, without deleting either. |
| 2 | lint enforcement | landed (PR #37) | "No bare strings" rule had no enforcement beyond `missing_enum_constant_in_switch`, which can't catch a switch that was never typed on an enum. Added `tool/claudart_lints` custom_lint plugin, rule `bare_string_for_enum`. |
| 3 | `bin/claudart.dart` | open | The command dispatch switch is exactly what rule #2 now flags — 21 subcommands switched on raw strings, 6 of which (`suggest`/`debug`/`setup`/`save`/`teardown`/`flow`) duplicate `AgentFlow`'s enum variants as a second, unlinked bare-string source. Needs a `ClaudartCommand`-style enum covering all 21, or reuse of `AgentFlow` extended to cover the non-pipeline commands too. |
| 4 | `kill.dart:61-64` | done (this branch, `4c7caff`) | Fixed the same way as #1 — also checks `dirExists` alongside `linkExists`. Regression test confirmed red on pre-fix code (`git stash`) before confirming green. |
| 5 | honored workspace `knowledge/` | open | `~/dev/dev_tools/claude/claudart/knowledge/` doesn't exist. `workspace.json.session.knowledge` lists 4 files (`dart_flutter`, `testing`, `enum-vs-variable`, `git-authorship`) that resolve nowhere. `/setup` (Agent 1, scaffold compiler) would fail or produce an empty scaffold if run today. |
| 6 | scaffold.md protocol | deciding | `docs/design.md` formally proves an Agent-1/Agent-2 split (`K_scaffold` vs `K_session`) built around `/setup` compiling `scaffold.md`. After fix #1, every command template (bare and suffixed) reads `handoff.md`/`skills.md` directly with zero `scaffold.md` dependency. Is the scaffold protocol still intended, or is `docs/design.md`'s proof describing a design that's now dead in practice? Needs a decision before anything here gets "fixed" — could go either way (revive scaffold.md properly, or retire the proof). |
| 7 | `good_intentions` (jolexxa) | parked | Researched: static analyzer-based architecture-layer validation (view→viewModel→useCase→repository→dataSource) + PlantUML diagram, via a Dart build hook. Blocked: requires `sdk: ^3.11.0`, this machine has 3.8.1. Also a layering mismatch — none of claudart (CLI), dartrix (test matrix), or zedup (bloc-free nocterm TUI) are shaped like the CRUD/bloc apps this tool targets. Not adopting as-is. |
| 8 | dartrix `PARADIGMS.md` PR | deciding, agreed direction | Architecture/paradigm ownership belongs to dartrix (`PARADIGMS.md` already says so explicitly: "dartrix — the law... claudart — applies the law"). If dependency-layering validation (the useful idea inside finding #7) is wanted, it must land as a proposed rule under dartrix's `architecture` dimension (or a new dimension) per `PARADIGMS.md`'s own Growth section, not invented unilaterally in claudart. PR not yet drafted. |
| 9 | `StackType`/`ProofNotation` → paradigm-package resolution | open | `lib/workspace/workspace_config.dart` already types `StackType` (dart/flutter/bloc/riverpod/typescript/react/swift/kotlin) and `ProofNotation` (dartGrounded/tsGrounded/generic) — the multi-language extension point already exists, but is completely unwired. `WorkspaceConfig.load()` is called in `suggest.dart`/`debug.dart`/`flow.dart`/`rotate.dart`/`scan.dart`, yet only `.owner.strict` is ever read — `.project.stack` drives nothing. Templates (`suggest_template.dart` etc.) hardcode `knowledge/generic/dart.md`/`testing.md` regardless of stack. No concept exists yet of "which package owns the paradigm for this stack" (dartrix for dart/flutter/bloc/riverpod; nothing modeled for typescript/python/etc.). Needed for "a new workspace requires suggest for Python" to actually work. |
| 10 | duplicate `suggest` implementations | done (this branch, `02fb065`) | Confirmed: `runSuggest` (real Dart pipeline) and `/suggest-claudart` (interactive editor skill) are genuinely two separate paths to the same outcome, not a bug. Investigating this surfaced the real gap: `suggest.dart`/`debug.dart`/`flow.dart` — the three commands making real Claude API calls — had zero test coverage and no injectable I/O seam for their `stdin.readLineSync()`/`arrowMenu()` calls. Closed for all three: added `confirmFn`/`promptFn`/`pickFn` params matching `setup.dart`'s existing pattern, then `suggest_test.dart`/`debug_test.dart`/`flow_test.dart` covering validation exits, confirmation-gate behavior, and reader-produces-nothing. Still not covered anywhere: the full review/apply loop past a successful pipeline run, and `flow.dart`'s checkpoint-resume branch (reads a real `File` off disk, not through `FileIO` — separate, pre-existing gap, noted but not fixed). |
| 11 | `logic_blocks` (jolexxa/bestie) | researched, split recommendation | Hierarchical statechart library, SDK-compatible (`>=3.5.0 <4.0.0`, unlike finding #7). Doesn't fit claudart's CLI — every command is a fresh short-lived process, state must be durable/file-backed for that reason, and LogicBlocks assumes a persistent in-memory instance. Strong fit for **zedup's TUI** specifically — long-running `nocterm` process, and `bestie` (same framework) already has a reusable `logic_bloc_adapter` reference implementation. Recommendation: don't touch claudart's `HandoffStatus`/`SessionState` model; consider `logic_blocks` for zedup's pane/session view state instead. |
| 12 | workspace-config → state-object idea | deciding | User's original ask ("once claudart is linked, a config communicates workspace state, updates a state object on construction") splits into two different targets depending on scope: the CLI's `WorkspaceConfig`/`SessionState` (durable-file model — see #9 for the actual gap there) vs. zedup's live view state (where #11's `logic_blocks` recommendation applies). Needs a decision on which one before scoping further. |
| 13 | knowledge/generic cross-ownership | open | `knowledge/generic/dart_flutter.md` (under `$CLAUDART_WORKSPACE/knowledge/generic/`, one shared file, not per-project) is referenced by both claudart's `workspace.json` (liitx-owned) and dc-flutter's (Toyota-owned). Content is currently clean, but `/teardown`'s "generic vs project-specific" classification is an ad-hoc LLM judgment call with no ownership/handle check — nothing prevents a Toyota-owned session's teardown from writing into the same shared file a liitx-owned session later reads, or vice versa. No schema, no per-entry provenance, not even git-tracked. |
| 14 | jev / TypeSafe AI ("System One") | researched, parked + alternative recommended | Investigated at the actual product level (typesafe.ai), not just the colleague's Dart wrapper. Real, distinct capability: a purpose-built model trained via Reinforcement Learning for Calibrated Decisions (not RLHF) emitting typed decisions with *calibrated* confidence scores, for autonomous-action-vs-escalate-to-human decisions — not fully deterministic, and not something claudart's `categorization.dart` already replicates (its classify step has no confidence signal at all). Blocked from adoption: requires `sdk: ^3.13.2` (machine has 3.8.1), and it's a third-party hosted API — sending workspace content off-machine directly conflicts with `sensitivityMode: true` on this workspace. Local-model-training version of the idea (train weights from a workspace's own handoffs) is impractical — this workspace has ~6 resolved sessions total, nowhere near a training set. Recommended alternative instead: no training, no gradient descent — embed `skills.md`'s existing Root Cause Patterns / Session Index entries and use cosine-similarity nearest-neighbor lookup as a Coordinator decision signal (state-transition gating, subagent-output-vs-precedent matching). Extends infrastructure that already exists and already grows every `teardown`, improves as sessions accumulate, no per-workspace data-volume problem. |
| 15 | `ConfirmationOption` protocol | landed (this branch) | Shared enum + wire-tag protocol (`lib/pipeline/agents/confirmation.dart`) so confirm/modify/clarify/reject gates parse identically across every consumer. Wired into `save`/`teardown`/`suggest` templates. New `claudart confirm-pending` CLI command + `PendingConfirmation` file (`pending_confirmation.json`, workspace-scoped, durable) close the gap zedup's stateless per-message dispatch has — a pending question no longer disappears into a `AgentContext.free` reply with no memory of what was asked. zedup-side `ContextRouter`/`AgentDispatcher` wiring to actually consume it is still open. |
| 16 | `enum_values_loop_in_single_test` lint | landed (this branch) | Second custom_lint rule (`tool/claudart_lints`), same mechanism as `bare_string_for_enum`. Flags a `for` loop over `SomeEnum.values` nested inside one `test()` body — found and fixed 6 pre-existing violations across `planner_log_test.dart`, `categorize_prompt_test.dart` (×3), `route_tag_test.dart` (×2). Belongs to dartrix's `testing` dimension per governance already established (finding #8) — candidate for migration to a dartrix-owned lint package alongside `bare_string_for_enum`, not yet moved. |
| 17 | `dart format` drift | open, not urgent | `dart format --set-exit-if-changed` disagrees with 58 of 91 files — the codebase hand-aligns `=>` arrows and trailing comments in columns, which the formatter collapses to single spaces. Zero runtime/correctness effect (whitespace is non-semantic in Dart) — this is a tooling-consistency question, not a bug. No CI enforces this today. Decision needed eventually: run `dart format -w .` once and adopt canonical formatting going forward, or keep the manual alignment and never add a format CI gate (Dart's formatter isn't configurable — no middle ground). Deferred — user wants to handle locally first, not adding CI yet. |
| 18 | `very_good_analysis` adoption | researched, not adopted | Dry-run against the whole repo: 2044 issues (0 errors, 3 pre-existing/harmless warnings, 2041 info). Top rules: `public_member_api_docs` (546), `avoid_print` (341 — actively wrong for claudart, a CLI whose output *is* print), `always_use_package_imports` (306). Not a clean swap — would need cherry-picking (take `directives_ordering`/`cascade_invocations`/`sort_constructors_first`, explicitly disable `avoid_print`) rather than wholesale `include:`. Also version-pinned: 9.0.0 works with the installed SDK (3.8.1); 10+ needs `^3.9.0`+. VGV's testing philosophy (fetched from engineering.verygood.ventures) already matches most of what claudart does today (test/ mirrors lib/, `_test.dart` suffix, one-scenario-per-test — the exact principle behind finding #16); their 4-layer architecture doctrine (Data→Repository→BusinessLogic→Presentation) doesn't fit claudart/dartrix/zedup, same conclusion as `good_intentions` (#7). |
| 19 | Coverage tracking | deferred to dartrix/shoelace | VGV's testing standard targets 100% coverage; claudart tracks none today. Decided: this belongs to dartrix's `shoelace` visualizer (WIP) once it's ready to consume coverage data, not a separate tool claudart should build itself. |
| 20 | PR #37 Copilot review | done (this branch, `5a303cb`) | Automated review left 2 findings. #1 ("move severity to `custom_lint.rules`, `analyzer.errors` likely ignored"): verified empirically — removing `analyzer.errors` dropped severity from ERROR to INFO, proving it *does* apply; pub.dev's own docs show no alternative severity syntax under `custom_lint.rules` to move to. Not a bug, no change made. #2 ("switch expressions bypass the statement-only check"): real, fixed — `bare_string_for_enum` now also checks `SwitchExpression`, but only flags when a case body performs an action (method/function call, await), distinguishing illegitimate dispatch from legitimate `fromString`-style enum-translation factories (which are also switch expressions with string-literal cases, and are the *correct* home for that pattern). Verified: 0 new findings across the real codebase's actual `fromString` factories, catches a scratch bypass-attempt file. |
| 21 | `ungrouped_identical_switch_cases` lint | done (this branch, `6091e29`) | Third custom_lint rule, per dartrix's new `architecture` paradigm (dartrix PR #28, third addition): identical switch-case bodies must group with `\|\|`, not repeat. Found and fixed 39 pre-existing violations across 6 files. `categorization.dart`'s `routeModel` grouping verified semantically identical via the existing 121-test exhaustive matrix (unchanged pass). `workspace_config.dart`'s `WorkspaceRole.fromString` had a genuinely redundant case (already subsumed by `_`) — deleted rather than grouped. |

## Topology notes (reference, not action items)

- `link` writes exactly two things: `registry.json` (durable addressing —
  `projectRoot → workspacePath`, re-read by every other command) and
  `.claude/commands/*.md` (frozen template snapshot, only updated when
  `link` runs again — paths are string-interpolated in at generation time,
  not resolved live).
- Session *state* (`HandoffStatus`) lives entirely in `handoff.md`, never
  touched by `link`.
- `AgentFlow` enum is the source of truth for the 6 pipeline flows
  (`suggest`, `debug`, `setup`, `save`, `teardown`, `flow`) — model,
  steps, and command-file template all keyed off one enum. The other 15
  CLI commands have no equivalent typed model (see finding #3).

## Next up

Going through the command list in order (per `bin/claudart.dart`'s own
usage string) to audit each one the same way `link` was audited — metadata
in, template/state out. `setup` is next.
